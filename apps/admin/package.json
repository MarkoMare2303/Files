{
  "name": "@swissov/admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p ${ADMIN_PORT:-3000}",
    "build": "next build",
    "start": "next start -p ${ADMIN_PORT:-3000}",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint app lib --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run --passWithNoTests",
    "clean": "rm -rf .next .turbo dist"
  },
  "dependencies": {
    "@supabase/ssr": "^0.5.2",
    "@supabase/supabase-js": "^2.47.10",
    "@swissov/types": "workspace:*",
    "@swissov/ui": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
